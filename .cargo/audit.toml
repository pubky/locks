[advisories]
# `rsa 0.9.10` is retained through SQLx's optional MySQL dependency closure.
# This workspace enables PostgreSQL only. Verify the exception remains unreachable with:
# cargo tree --workspace --all-features --target all -i rsa@0.9.10
ignore = ["RUSTSEC-2023-0071"]
